name: docs

channels:

  - conda-forge
  - defaults

dependencies:

  # Write here all dependencies to compile the sphinx documentation.
  # This list is just an example
  - python=3.6
  - sphinx=3.5.4
  - sphinx_rtd_theme
  - sphinx-prompt==1.4.0